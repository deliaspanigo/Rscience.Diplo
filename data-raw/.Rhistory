rm(nombre_en_R)
}
output_folder <- "../data"
selected_file <- "SEMANA01_BASE01_PESOS.csv"
selected_sep <- ";"
selected_dec <- ","
cargar_base_package(output_folder, selected_file, selected_sep, selected_dec )
# Funcion para cargar
cargar_base_package <- function(output_folder, selected_file, selected_sep, selected_dec ) {
# Construir la ruta completa al archivo CSV
output_path <- file.path(output_folder, selected_file)
# Leer el archivo CSV desde la ruta completa
base_cargada <- read.csv(file = selected_file, header = TRUE, sep = selected_sep, dec = selected_dec)
# Obtener el nombre del objeto sin la extensión
nombre_en_R <- tools::file_path_sans_ext(selected_file)
# Asignar el objeto a un nombre en el entorno global
assign(nombre_en_R, base_cargada, envir = .GlobalEnv)
# Utilizar usethis::use_data para registrar el objeto en el paquete
nueva_sentencia <- paste0("usethis::use_data(", nombre_en_R, ", overwrite = TRUE)")
eval(parse(text = nueva_sentencia))
# Limpiar objetos innecesarios
rm(base_cargada)
rm(nombre_en_R)
}
with({
output_folder <- "../data"
selected_file <- "SEMANA01_BASE01_PESOS.csv"
selected_sep <- ";"
selected_dec <- ","
}, cargar_base_package(output_folder, selected_file, selected_sep, selected_dec ))
output_folder <- "../data"
selected_file <- "SEMANA01_BASE01_PESOS.csv"
selected_sep <- ";"
selected_dec <- ","
cargar_base_package(output_folder, selected_file, selected_sep, selected_dec )
# Funcion para cargar
cargar_base_package <- function(output_folder, selected_file, selected_sep, selected_dec ) {
# Construir la ruta completa al archivo CSV
output_path <- file.path(output_folder, selected_file)
# Leer el archivo CSV desde la ruta completa
base_cargada <- read.csv(file = selected_file, header = TRUE, sep = selected_sep, dec = selected_dec)
# Obtener el nombre del objeto sin la extensión
nombre_en_R <- tools::file_path_sans_ext(selected_file)
# Asignar el objeto a un nombre en el entorno global
assign(nombre_en_R, base_cargada, envir = .GlobalEnv)
# Utilizar usethis::use_data para registrar el objeto en el paquete
nueva_sentencia <- paste0("usethis::use_data(", nombre_en_R, ", overwrite = TRUE)")
eval(parse(text = nueva_sentencia))
# Limpiar objetos innecesarios
rm(base_cargada)
rm(nombre_en_R)
}
with({
output_folder <- "../data"
selected_file <- "SEMANA01_BASE01_PESOS.csv"
selected_sep <- ";"
selected_dec <- ","
cargar_base_package(output_folder, selected_file, selected_sep, selected_dec )
})
# Funcion para cargar
cargar_base_package <- function(output_folder, selected_file, selected_sep, selected_dec ) {
# Construir la ruta completa al archivo CSV
output_path <- file.path(output_folder, selected_file)
# Leer el archivo CSV desde la ruta completa
base_cargada <- read.csv(file = selected_file, header = TRUE, sep = selected_sep, dec = selected_dec)
# Obtener el nombre del objeto sin la extensión
nombre_en_R <- tools::file_path_sans_ext(selected_file)
# Asignar el objeto a un nombre en el entorno global
assign(nombre_en_R, base_cargada, envir = .GlobalEnv)
# Utilizar usethis::use_data para registrar el objeto en el paquete
nueva_sentencia <- paste0("usethis::use_data(", nombre_en_R, ", overwrite = TRUE)")
eval(parse(text = nueva_sentencia))
# Limpiar objetos innecesarios
rm(base_cargada)
rm(nombre_en_R)
}
with(
{
output_folder <- "../data"
selected_file <- "SEMANA01_BASE01_PESOS.csv"
selected_sep <- ";"
selected_dec <- ","
},
cargar_base_package(output_folder, selected_file, selected_sep, selected_dec)
)
# Funcion para cargar
cargar_base_package <- function(output_folder, selected_file, selected_sep, selected_dec ) {
# Construir la ruta completa al archivo CSV
output_path <- file.path(output_folder, selected_file)
# Leer el archivo CSV desde la ruta completa
base_cargada <- read.csv(file = selected_file, header = TRUE, sep = selected_sep, dec = selected_dec)
# Obtener el nombre del objeto sin la extensión
nombre_en_R <- tools::file_path_sans_ext(selected_file)
# Asignar el objeto a un nombre en el entorno global
assign(nombre_en_R, base_cargada)
# Utilizar usethis::use_data para registrar el objeto en el paquete
nueva_sentencia <- paste0("usethis::use_data(", nombre_en_R, ", overwrite = TRUE)")
eval(parse(text = nueva_sentencia))
# Limpiar objetos innecesarios
rm(base_cargada)
rm(nombre_en_R)
}
with(
{
output_folder <- "../data"
selected_file <- "SEMANA01_BASE01_PESOS.csv"
selected_sep <- ";"
selected_dec <- ","
},
cargar_base_package(output_folder, selected_file, selected_sep, selected_dec)
)
# Funcion para cargar
cargar_base_package <- function(output_folder, selected_file, selected_sep, selected_dec ) {
# Construir la ruta completa al archivo CSV
output_path <- file.path(output_folder, selected_file)
# Leer el archivo CSV desde la ruta completa
base_cargada <- read.csv(file = selected_file, header = TRUE, sep = selected_sep, dec = selected_dec)
# Obtener el nombre del objeto sin la extensión
nombre_en_R <- tools::file_path_sans_ext(selected_file)
# Asignar el objeto a un nombre en el entorno global
assign(nombre_en_R, base_cargada)
# Utilizar usethis::use_data para registrar el objeto en el paquete
nueva_sentencia <- paste0("usethis::use_data(", nombre_en_R, ", overwrite = TRUE)")
eval(parse(text = nueva_sentencia))
# Limpiar objetos innecesarios
rm(base_cargada)
rm(nombre_en_R)
}
with(
{
output_folder <- "../data"
selected_file <- "SEMANA01_BASE01_PESOS.csv"
selected_sep <- ";"
selected_dec <- ","
},
cargar_base_package(output_folder, selected_file, selected_sep, selected_dec)
)
# Funcion para cargar
cargar_base_package <- function(output_folder, selected_file, selected_sep, selected_dec ) {
# Construir la ruta completa al archivo CSV
output_path <- file.path(output_folder, selected_file)
# Leer el archivo CSV desde la ruta completa
base_cargada <- read.csv(file = selected_file, header = TRUE, sep = selected_sep, dec = selected_dec)
# Obtener el nombre del objeto sin la extensión
nombre_en_R <- tools::file_path_sans_ext(selected_file)
# Asignar el objeto a un nombre en el entorno global
assign(nombre_en_R, base_cargada)
# Utilizar usethis::use_data para registrar el objeto en el paquete
nueva_sentencia <- paste0("usethis::use_data(", nombre_en_R, ", overwrite = TRUE)")
eval(parse(text = nueva_sentencia))
# Limpiar objetos innecesarios
rm(base_cargada)
rm(nombre_en_R)
}
nuevo_entorno <- new.env()
with(
{
output_folder <- "../data"
selected_file <- "SEMANA01_BASE01_PESOS.csv"
selected_sep <- ";"
selected_dec <- ","
},
cargar_base_package(output_folder, selected_file, selected_sep, selected_dec)
)
# Funcion para cargar
cargar_base_package <- function(output_folder, selected_file, selected_sep, selected_dec ) {
# Construir la ruta completa al archivo CSV
output_path <- file.path(output_folder, selected_file)
# Leer el archivo CSV desde la ruta completa
base_cargada <- read.csv(file = selected_file, header = TRUE, sep = selected_sep, dec = selected_dec)
# Obtener el nombre del objeto sin la extensión
nombre_en_R <- tools::file_path_sans_ext(selected_file)
# Asignar el objeto a un nombre en el entorno global
assign(nombre_en_R, base_cargada)
# Utilizar usethis::use_data para registrar el objeto en el paquete
nueva_sentencia <- paste0("usethis::use_data(", nombre_en_R, ", overwrite = TRUE)")
eval(parse(text = nueva_sentencia))
# Limpiar objetos innecesarios
rm(base_cargada)
rm(nombre_en_R)
}
with(
{
nuevo_entorno <- new.env()
output_folder <- "../data"
selected_file <- "SEMANA01_BASE01_PESOS.csv"
selected_sep <- ";"
selected_dec <- ","
},
cargar_base_package(output_folder, selected_file, selected_sep, selected_dec)
)
# Funcion para cargar
cargar_base_package <- function(output_folder, selected_file, selected_sep, selected_dec ) {
nuevo_entorno <- new.env()
# Construir la ruta completa al archivo CSV
output_path <- file.path(output_folder, selected_file)
# Leer el archivo CSV desde la ruta completa
base_cargada <- read.csv(file = selected_file, header = TRUE, sep = selected_sep, dec = selected_dec)
# Obtener el nombre del objeto sin la extensión
nombre_en_R <- tools::file_path_sans_ext(selected_file)
# Asignar el objeto a un nombre en el entorno global
assign(nombre_en_R, base_cargada)
# Utilizar usethis::use_data para registrar el objeto en el paquete
nueva_sentencia <- paste0("usethis::use_data(", nombre_en_R, ", overwrite = TRUE)")
eval(parse(text = nueva_sentencia))
# Limpiar objetos innecesarios
rm(base_cargada)
rm(nombre_en_R)
}
with(
{
output_folder <- "../data"
selected_file <- "SEMANA01_BASE01_PESOS.csv"
selected_sep <- ";"
selected_dec <- ","
},
cargar_base_package(output_folder, selected_file, selected_sep, selected_dec)
)
# Funcion para cargar
cargar_base_package <- function(output_folder, selected_file, selected_sep, selected_dec ) {
# Construir la ruta completa al archivo CSV
output_path <- file.path(output_folder, selected_file)
# Leer el archivo CSV desde la ruta completa
base_cargada <- read.csv(file = selected_file, header = TRUE, sep = selected_sep, dec = selected_dec)
# Obtener el nombre del objeto sin la extensión
nombre_en_R <- tools::file_path_sans_ext(selected_file)
# Asignar el objeto a un nombre en el entorno global
assign(nombre_en_R, base_cargada)
# Utilizar usethis::use_data para registrar el objeto en el paquete
nueva_sentencia <- paste0("usethis::use_data(", nombre_en_R, ", overwrite = TRUE)")
eval(parse(text = nueva_sentencia))
# Limpiar objetos innecesarios
rm(base_cargada)
rm(nombre_en_R)
}
# Crear un nuevo entorno
nuevo_entorno <- new.env()
# Definir las variables necesarias en el nuevo entorno
nuevo_entorno$output_folder <- "../data"
nuevo_entorno$selected_file <- "SEMANA01_BASE01_PESOS.csv"
nuevo_entorno$selected_sep <- ";"
nuevo_entorno$selected_dec <- ","
# Ejecutar la función cargar_base_package() dentro del nuevo entorno
with(nuevo_entorno, cargar_base_package(output_folder, selected_file, selected_sep, selected_dec))
# Ejecutar la función cargar_base_package() dentro del nuevo entorno
with(nuevo_entorno, {
output_folder <- "../data"
selected_file <- "SEMANA01_BASE01_PESOS.csv"
selected_sep <- ";"
selected_dec <- ","
}
cargar_base_package(output_folder, selected_file, selected_sep, selected_dec))
# Funcion para cargar
cargar_base_package <- function(output_folder, selected_file, selected_sep, selected_dec ) {
# Construir la ruta completa al archivo CSV
output_path <- file.path(output_folder, selected_file)
# Leer el archivo CSV desde la ruta completa
base_cargada <- read.csv(file = selected_file, header = TRUE, sep = selected_sep, dec = selected_dec)
# Obtener el nombre del objeto sin la extensión
nombre_en_R <- tools::file_path_sans_ext(selected_file)
# Asignar el objeto a un nombre en el entorno global
assign(nombre_en_R, base_cargada)
# Utilizar usethis::use_data para registrar el objeto en el paquete
nueva_sentencia <- paste0("usethis::use_data(", nombre_en_R, ", overwrite = TRUE)")
eval(parse(text = nueva_sentencia))
# Limpiar objetos innecesarios
rm(base_cargada)
rm(nombre_en_R)
}
# Crear un nuevo entorno
nuevo_entorno <- new.env()
# Ejecutar la función cargar_base_package() dentro del nuevo entorno
with(nuevo_entorno, {
output_folder <- "../data"
selected_file <- "SEMANA01_BASE01_PESOS.csv"
selected_sep <- ";"
selected_dec <- ","
}
cargar_base_package(output_folder, selected_file, selected_sep, selected_dec))
# Funcion para cargar
cargar_base_package <- function(output_folder, selected_file, selected_sep, selected_dec ) {
# Construir la ruta completa al archivo CSV
output_path <- file.path(output_folder, selected_file)
# Leer el archivo CSV desde la ruta completa
base_cargada <- read.csv(file = selected_file, header = TRUE, sep = selected_sep, dec = selected_dec)
# Obtener el nombre del objeto sin la extensión
nombre_en_R <- tools::file_path_sans_ext(selected_file)
# Asignar el objeto a un nombre en el entorno global
assign(nombre_en_R, base_cargada)
# Utilizar usethis::use_data para registrar el objeto en el paquete
nueva_sentencia <- paste0("usethis::use_data(", nombre_en_R, ", overwrite = TRUE)")
eval(parse(text = nueva_sentencia))
# Limpiar objetos innecesarios
rm(base_cargada)
rm(nombre_en_R)
}
# Crear un nuevo entorno
#nuevo_entorno <- new.env()
# Definir las variables necesarias en el nuevo entorno
nuevo_entorno$output_folder <- "../data"
nuevo_entorno$selected_file <- "SEMANA01_BASE01_PESOS.csv"
nuevo_entorno$selected_sep <- ";"
nuevo_entorno$selected_dec <- ","
# Ejecutar la función cargar_base_package() dentro del nuevo entorno
with(nuevo_entorno, {
output_folder <- "../data"
selected_file <- "SEMANA01_BASE01_PESOS.csv"
selected_sep <- ";"
selected_dec <- ","
}
cargar_base_package(output_folder, selected_file, selected_sep, selected_dec))
# Funcion para cargar
cargar_base_package <- function(output_folder, selected_file, selected_sep, selected_dec ) {
# Construir la ruta completa al archivo CSV
output_path <- file.path(output_folder, selected_file)
# Leer el archivo CSV desde la ruta completa
base_cargada <- read.csv(file = selected_file, header = TRUE, sep = selected_sep, dec = selected_dec)
# Obtener el nombre del objeto sin la extensión
nombre_en_R <- tools::file_path_sans_ext(selected_file)
# Asignar el objeto a un nombre en el entorno global
assign(nombre_en_R, base_cargada)
# Utilizar usethis::use_data para registrar el objeto en el paquete
nueva_sentencia <- paste0("usethis::use_data(", nombre_en_R, ", overwrite = TRUE)")
eval(parse(text = nueva_sentencia))
# Limpiar objetos innecesarios
rm(base_cargada)
rm(nombre_en_R)
}
# Crear un nuevo entorno
#nuevo_entorno <- new.env()
# Definir las variables necesarias en el nuevo entorno
nuevo_entorno$output_folder <- "../data"
nuevo_entorno$selected_file <- "SEMANA01_BASE01_PESOS.csv"
nuevo_entorno$selected_sep <- ";"
nuevo_entorno$selected_dec <- ","
with(
list(
output_folder = "../data",
selected_file = "SEMANA01_BASE01_PESOS.csv",
selected_sep = ";",
selected_dec = ","
),
cargar_base_package(output_folder, selected_file, selected_sep, selected_dec)
)
# # # Cargamos la SEMANA01_BASE02_PESOS.csv
with(
list(
output_folder = "../data",
selected_file = "SEMANA01_BASE02_ALTURA.csv",
selected_sep = ";",
selected_dec = "."
),
cargar_base_package(output_folder, selected_file, selected_sep, selected_dec)
)
# Funcion para cargar
cargar_base_package <- function(output_folder, selected_file, selected_sep, selected_dec ) {
# Construir la ruta completa al archivo CSV
output_path <- file.path(output_folder, selected_file)
# Leer el archivo CSV desde la ruta completa
base_cargada <- read.csv(file = selected_file, header = TRUE, sep = selected_sep, dec = selected_dec)
# Obtener el nombre del objeto sin la extensión
nombre_en_R <- tools::file_path_sans_ext(selected_file)
# Asignar el objeto a un nombre en el entorno global
assign(nombre_en_R, base_cargada)
# Utilizar usethis::use_data para registrar el objeto en el paquete
nueva_sentencia <- paste0("usethis::use_data(", nombre_en_R, ", overwrite = TRUE)")
eval(parse(text = nueva_sentencia))
# Limpiar objetos innecesarios
rm(base_cargada)
rm(nombre_en_R)
}
# # # Cargamos la SEMANA01_BASE01_PESOS.csv
with(
list(
output_folder = "../data",
selected_file = "SEMANA01_BASE01_PESOS.csv",
selected_sep = ";",
selected_dec = ","
),
cargar_base_package(output_folder, selected_file, selected_sep, selected_dec)
)
#####################################################################################
# # # Cargamos la SEMANA01_BASE02_PESOS.csv
with(
list(
output_folder = "../data",
selected_file = "SEMANA01_BASE02_ALTURA.csv",
selected_sep = ";",
selected_dec = "."
),
cargar_base_package(output_folder, selected_file, selected_sep, selected_dec)
)
#####################################################################################
# Funcion para cargar
cargar_base_package <- function(output_folder, selected_file, selected_sep, selected_dec ) {
# Construir la ruta completa al archivo CSV
output_path <- file.path(output_folder, selected_file)
# Leer el archivo CSV desde la ruta completa
base_cargada <- read.csv(file = selected_file, header = TRUE, sep = selected_sep, dec = selected_dec)
# Obtener el nombre del objeto sin la extensión
nombre_en_R <- tools::file_path_sans_ext(selected_file)
# Asignar el objeto a un nombre en el entorno global
assign(nombre_en_R, base_cargada)
# Utilizar usethis::use_data para registrar el objeto en el paquete
nueva_sentencia <- paste0("usethis::use_data(", nombre_en_R, ", overwrite = TRUE)")
eval(parse(text = nueva_sentencia))
# Limpiar objetos innecesarios
rm(base_cargada)
rm(nombre_en_R)
}
# # # Cargamos la SEMANA01_BASE01_PESOS.csv
with(
list(
output_folder = "../data",
selected_file = "SEMANA01_BASE01_PESOS.csv",
selected_sep = ";",
selected_dec = ","
),
cargar_base_package(output_folder, selected_file, selected_sep, selected_dec)
)
#####################################################################################
# # # Cargamos la SEMANA01_BASE02_PESOS.csv
with(
list(
output_folder = "../../data",
selected_file = "SEMANA01_BASE02_ALTURA.csv",
selected_sep = ";",
selected_dec = "."
),
cargar_base_package(output_folder, selected_file, selected_sep, selected_dec)
)
#####################################################################################
# Funcion para cargar
cargar_base_package <- function(output_folder, selected_file, selected_sep, selected_dec ) {
# Construir la ruta completa al archivo CSV
output_path <- file.path(output_folder, selected_file)
# Leer el archivo CSV desde la ruta completa
base_cargada <- read.csv(file = selected_file, header = TRUE, sep = selected_sep, dec = selected_dec)
# Obtener el nombre del objeto sin la extensión
nombre_en_R <- tools::file_path_sans_ext(selected_file)
# Asignar el objeto a un nombre en el entorno global
assign(nombre_en_R, base_cargada)
# Utilizar usethis::use_data para registrar el objeto en el paquete
nueva_sentencia <- paste0("usethis::use_data(", nombre_en_R, ", overwrite = TRUE)")
eval(parse(text = nueva_sentencia))
# Limpiar objetos innecesarios
rm(base_cargada)
rm(nombre_en_R)
}
# # # Cargamos la SEMANA01_BASE01_PESOS.csv
with(
list(
output_folder = "../../data",
selected_file = "SEMANA01_BASE01_PESOS.csv",
selected_sep = ";",
selected_dec = ","
),
cargar_base_package(output_folder, selected_file, selected_sep, selected_dec)
)
#####################################################################################
# # # Cargamos la SEMANA01_BASE02_PESOS.csv
with(
list(
output_folder = "../../data",
selected_file = "SEMANA01_BASE02_ALTURA.csv",
selected_sep = ";",
selected_dec = "."
),
cargar_base_package(output_folder, selected_file, selected_sep, selected_dec)
)
#####################################################################################
# Funcion para cargar
cargar_base_package <- function(output_folder, selected_file, selected_sep, selected_dec ) {
# Construir la ruta completa al archivo CSV
output_path <- file.path(output_folder, selected_file)
# Leer el archivo CSV desde la ruta completa
base_cargada <- read.csv(file = selected_file, header = TRUE, sep = selected_sep, dec = selected_dec)
# Obtener el nombre del objeto sin la extensión
nombre_en_R <- tools::file_path_sans_ext(selected_file)
# Asignar el objeto a un nombre en el entorno global
assign(nombre_en_R, base_cargada)
# Utilizar usethis::use_data para registrar el objeto en el paquete
nueva_sentencia <- paste0("usethis::use_data(", nombre_en_R, ", overwrite = TRUE)")
eval(parse(text = nueva_sentencia))
# Limpiar objetos innecesarios
rm(base_cargada)
rm(nombre_en_R)
}
# # # Cargamos la SEMANA01_BASE01_PESOS.csv
#with(
#list(
output_folder = "../data"
selected_file = "SEMANA01_BASE01_PESOS.csv"
selected_sep = ";"
selected_dec = ","
#),
cargar_base_package(output_folder, selected_file, selected_sep, selected_dec)
#)
#####################################################################################
# # # Cargamos la SEMANA01_BASE02_PESOS.csv
#with(
# list(
output_folder = "../data"
selected_file = "SEMANA01_BASE02_ALTURA.csv"
selected_sep = ";"
selected_dec = "."
#),
cargar_base_package(output_folder, selected_file, selected_sep, selected_dec)
#)
#####################################################################################
getwd()
list.files("../")
